System & Role: You are an expert AI Research Engineer assisting me in debugging a custom reinforcement learning environment. We are reimplementing the paper "Graph-Convolutional-Network-Enabled Task Offloading for Industrial Image Recognition in Digital Twin Edge Networks" (specifically focusing on the $\epsilon$-ATN-MADDPG and GCN task prioritization).Problem Statement: My current environment implementation is not behaving as the authors described. I need your help to theoretically audit the project before we write any more code. Do NOT generate any code solutions yet. We need to align on the theory and identify discrepancies first.Tasks:Please carefully read the paper, paying special attention to the mathematical formulations in Section III (System Models), Section IV (Problem Formulation), and Section V.B (MDP Formulation). Then, provide the following:Implementation Audit: Based on my current workspace/files, identify what we have implemented incorrectly or mapped poorly from the paper's MDP definitions (State, Action Space, Reward function, and state transitions).The "Missing Pieces" Analysis: Research papers often omit specific hyperparameters or implementation details. List every element, variable, or parameter that is required to build this environment but is not explicitly defined in the paper (e.g., exact values for the reward weights $\lambda_1$ through $\lambda_5$, specific GCN layer configurations beyond the 32-dim hidden layer, observation space normalization techniques, etc.). Tell me exactly what we are currently being forced to "guess."Strategic TODO List: Give me a structured, step-by-step TODO list of what we need to fix in our environment logic to match the paper's theoretical constraints.